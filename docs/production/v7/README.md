# Live Production Apps Script v7 — provenance only

`Código.js` in this directory is the recovered live Production booking backend
(version 7). It is the Git baseline representation, not the clasp runtime for
the lifecycle V2 release candidate.

Clasp the modular files under `backend/appsscript/booking/` (`Code.js` and
siblings). Never push this monolith in the same project as `Code.js`.
